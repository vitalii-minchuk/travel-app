import React from "react"
import { wrapper } from "./List.styles"
import Box from "@mui/material/Box"

const List = () => {
  return (
    <React.Fragment>
      <Box sx={wrapper}>
        hel
      </Box>
    </React.Fragment>
  )
}

export default List